#include "main.h"

/**
 * print_line - prints a line uinsg _ based on a number given
 * @n: is an intger input
 * return: is a printout of _
 */
void print_line(int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		_putchar(95);
	}
_putchar('\n');
}

